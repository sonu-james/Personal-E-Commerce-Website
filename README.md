# Luna Fashion — Clean Next.js + Tailwind Starter

Only the basic Next.js project setup and Tailwind CSS are included.

The `components/` folder contains named placeholder files only. Build all components yourself.

## Run

npm install
npm run dev
